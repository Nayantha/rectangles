# Struct in Rust
Rectangle is used to Demonstrate
<br>
* **Define Struct** <br>
  #[derive(Debug)] can be used to easily print struct on the output / CMD.
```rust
struct Rectangle {
width: u32, // fields
height: u32,
}
```

